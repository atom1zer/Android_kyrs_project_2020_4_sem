<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kyrsovaya_client_v2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".NavigationActivity"
            android:label="@string/title_activity_navigation"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".activitys.LoginActivity"
            android:label="Login to your Account">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Entry for RegisterActivity.class -->
        <activity
            android:name=".activitys.RegisterActivity"
            android:label="Register New Account" />
        <activity android:name=".activitys.ChatListActivity" />
        <activity android:name=".activitys.QuizOnClickActivity" />
        <activity android:name=".activitys.PersonalQuizActivity" />
        <activity android:name=".activitys.AddQuizActivity" />
        <activity android:name=".activitys.StatActivity" />
    </application>

</manifest>